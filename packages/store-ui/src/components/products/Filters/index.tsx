import React from 'react';
import { Title } from '@lamk/blocks-ui';

export const Filters = ({}) => {
  return (
    <div>
      <Title mb={1} level={4}>
        Filter products
      </Title>
    </div>
  );
};
