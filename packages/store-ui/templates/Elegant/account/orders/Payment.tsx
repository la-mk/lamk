import React from "react";
import { useTranslation } from "next-i18next";
import { PayProps } from "../../../../containers/account/orders/Pay";

export const Payment = ({}: PayProps) => {
  const { t } = useTranslation("translation");
  return null;
};
