{
  "name": "lamk-aida",
  "version": "0.0.1",
  "description": "Lamk Aida Models",
  "author": "Stevche Radevski (sradevski@live.com)",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "lint": "eslint packages --fix ",
    "precommit": "lint-staged",
    "build": "babel Models/ --ignore node_modules,__tests__,__mocks__ --out-dir dist/"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "./**/*.{js,mjs,jsx,ts,tsx}": [
      "eslint --fix",
      "git add"
    ]
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-prettier": "^4.2.0",
    "eslint-plugin-prettier": "^3.0.1",
    "husky": "^2.2.0",
    "lint-staged": "^8.1.6",
    "prettier": "^1.17.0"
  }
}
